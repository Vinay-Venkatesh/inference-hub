{{- if and .Values.redis.openwebui.enabled (not .Values.redis.openwebui.external.enabled) .Values.redis.openwebui.auth.password }}
{{ include "inferencehub.redis.secret" (dict "app" "openwebui" "redis" .Values.redis.openwebui "context" .) }}
{{- end }}
---
{{- if and .Values.redis.litellm.enabled (not .Values.redis.litellm.external.enabled) .Values.redis.litellm.auth.password }}
{{ include "inferencehub.redis.secret" (dict "app" "litellm" "redis" .Values.redis.litellm "context" .) }}
{{- end }}
