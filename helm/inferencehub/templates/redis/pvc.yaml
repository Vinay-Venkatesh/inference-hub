{{- if and .Values.redis.openwebui.enabled (not .Values.redis.openwebui.external.enabled) .Values.redis.openwebui.persistence.enabled }}
{{ include "inferencehub.redis.pvc" (dict "app" "openwebui" "redis" .Values.redis.openwebui "context" .) }}
{{- end }}
---
{{- if and .Values.redis.litellm.enabled (not .Values.redis.litellm.external.enabled) .Values.redis.litellm.persistence.enabled }}
{{ include "inferencehub.redis.pvc" (dict "app" "litellm" "redis" .Values.redis.litellm "context" .) }}
{{- end }}
